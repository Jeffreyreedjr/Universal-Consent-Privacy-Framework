# Fonts

**Plus Jakarta Sans** (SIL Open Font License 1.1), latin weights 400–700.

Self-hosted for UCPF — do not replace with a CDN (no phone-home).

Source package: `@fontsource/plus-jakarta-sans` (copied at build time into this folder).
