# Scanner reports

Generated locally. Do not commit fleet coverage output.

```bash
npm run coverage:fleet
```

Writes `fleet-coverage.md` / `fleet-coverage.json` here (gitignored). Requires `data/fleet-inventory-normalized.json` (also gitignored under `data/`).
